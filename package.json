{
  "name": "camai-root",
  "version": "1.0.3",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix portal\" --names \"server,portal\" --prefix-colors \"cyan,magenta\"",
    "install:all": "npm install && npm install --prefix server && npm install --prefix portal",
    "build": "npm run build --prefix portal"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
