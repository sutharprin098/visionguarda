{
  "name": "camai-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm.cmd run dev --prefix server\" \"npm.cmd run dev --prefix client\" --names \"server,client\" --prefix-colors \"cyan,magenta\"",
    "install:all": "npm.cmd install && npm.cmd install --prefix server && npm.cmd install --prefix client",
    "build": "npm.cmd run build --prefix server && npm.cmd run build --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
