{
  "name": "camai-root",
  "version": "1.0.2",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm.cmd run dev --prefix server\" \"npm.cmd run dev --prefix portal\" --names \"server,portal\" --prefix-colors \"cyan,magenta\"",
    "install:all": "npm.cmd install && npm.cmd install --prefix server && npm.cmd install --prefix portal",
    "build": "npm.cmd run build --prefix server && npm.cmd run build --prefix portal"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
