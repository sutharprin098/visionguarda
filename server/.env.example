# CamAI AI Engine (FastAPI) — copy to server/.env and adjust.
# The engine binds loopback by default: it has no built-in auth and is meant
# to be consumed by the desktop app / local viewer on the same machine.
CAMAI_HOST=127.0.0.1
CAMAI_PORT=8000
